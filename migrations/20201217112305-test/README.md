# Migration `20201217112305-test`

This migration has been generated by Emile Fugulin at 12/17/2020, 11:23:05 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Calendar" (
"accessRole" text  NOT NULL ,"id" text  NOT NULL ,"provider" text  NOT NULL ,"providerId" text  NOT NULL ,"summary" text  NOT NULL ,"timeZone" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."CalendarEvent" (
"calendarId" text  NOT NULL ,"end" timestamp(3)  NOT NULL ,"id" text  NOT NULL ,"provider" text  NOT NULL ,"providerId" text  NOT NULL ,"start" timestamp(3)  NOT NULL ,"timeZone" text   ,
    PRIMARY KEY ("id"))

CREATE  INDEX "Calendar.providerId" ON "public"."Calendar"("providerId")

CREATE  INDEX "CalendarEvent.providerId" ON "public"."CalendarEvent"("providerId")

ALTER TABLE "public"."CalendarEvent" ADD FOREIGN KEY ("calendarId")REFERENCES "public"."Calendar"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20201217112305-test
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,33 @@
+datasource db {
+  provider = "postgresql"
+  url      = env("DATABASE_URL")
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model Calendar {
+  accessRole String
+  events     CalendarEvent[]
+  id         String          @id @default(cuid())
+  provider   String
+  providerId String
+  summary    String
+  timeZone   String
+
+  @@index([providerId])
+}
+
+model CalendarEvent {
+  calendar   Calendar @relation(fields: [calendarId], references: [id])
+  calendarId String
+  end        DateTime
+  id         String   @id @default(cuid())
+  provider   String
+  providerId String
+  start      DateTime
+  timeZone   String?
+
+  @@index([providerId])
+}
```


